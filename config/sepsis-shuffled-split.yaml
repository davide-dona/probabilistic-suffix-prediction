# Sepsis as U-ED-LSTM sees it: their split, their feature set. The same raw log as the `temporal`
# variant, preprocessed a second time so both descriptions can exist at once and our model can be
# scored against their checkpoint over the same test cases.
data:
  name: sepsis
  variant: shuffled-split

  case_key: "Case ID"
  activity_key: Activity
  resource_key: Resource
  timestamp_key: "time:timestamp"
  train_split: 0.65
  val_split: 0.15
  test_split: 0.2
  split_strategy: uedlstm_replicated
  max_seq_len_percentile: 98.5  # as in `sepsis.yaml`

  # Their v1 Sepsis loader notebook's lists, minus `concept:name` and `org:group`, which are the
  # activity and resource channels here, and minus `lifecycle:transition`, which this export of
  # the log does not carry. Age is a static categorical for them and stays numeric here: this
  # pipeline reads a column's kind off its dtype, and forcing it would be a knob for one column.
  event_features:
    - Diagnose
    - InfectionSuspected
    - DiagnosticArtAstrup
    - DiagnosticBlood
    - DiagnosticECG
    - DiagnosticIC
    - DiagnosticLacticAcid
    - DiagnosticLiquor
    - DiagnosticOther
    - DiagnosticSputum
    - DiagnosticUrinaryCulture
    - DiagnosticUrinarySediment
    - DiagnosticXthorax
    - DisfuncOrg
    - Hypotensie
    - Hypoxie
    - Infusion
    - Oligurie
    - SIRSCritHeartRate
    - SIRSCritLeucos
    - SIRSCritTachypnea
    - SIRSCritTemperature
    - SIRSCriteria2OrMore
    - Age
    - CRP
    - LacticAcid
    - Leucocytes
    - day_in_week
    - seconds_in_day

  # Their `num_dyn_log` is empty, so every numeric channel is standardized raw.
  log_scaled_features: []
  log_scaled_durations: false
  batch_size: 256
  num_workers: 0
